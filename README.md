# squadAgents
repository for squad agents development



## Setup

```bash
uv venv --python 3.12
source .venv/bin/activate
uv pip install -U agno openai duckduckgo-search elevenlabs sqlalchemy 'fastapi[standard]' lancedb pylance tantivy pandas numpy dotenv ddgs 
```